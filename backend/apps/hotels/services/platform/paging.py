"""
Пределы выдач платформенной консоли.

ПЕРЕЕХАЛО В `apps/core/listing`. Модуль заводился, когда пределы нужны были
только консоли; теперь общий контракт списков — поиск, фильтры, сортировка,
листание — один на оба приложения, и держать здесь вторую половину значило бы
ровно то, от чего этот модуль в своё время и спасал: два ответа на один вопрос.

Имена сохранены: их зовут семь мест консоли, и переписывать вызовы ради
переезда — менять работающее на одинаковое.
"""

from __future__ import annotations

from apps.core.listing import DEFAULT_LIMIT, MAX_LIMIT, clamp, envelope

__all__ = ["DEFAULT_LIMIT", "MAX_LIMIT", "clamp", "envelope"]
