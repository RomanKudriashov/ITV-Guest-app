"""
Сервисный слой аналитики: сбор событий, пересчёт витрин, запросы, выгрузки.

Реэкспорта нет: снаружи аналитику зовут по имени модуля (`queries.summary`),
и это ровно то, чего мы и хотим — импорт называет операцию.
"""
