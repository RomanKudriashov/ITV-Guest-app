"""
Сервисный слой каталога: вся работа с данными живёт здесь, вьюха её зовёт.

Разложено по операциям, а не по потребителю: `menu` собирает витрину гостю,
`cms` правит справочники из редактора, `availability` отвечает на один вопрос —
доступна ли позиция сейчас. Реэкспорта здесь намеренно нет: импорт должен
называть модуль, иначе через полгода снова получится одна свалка на 1500 строк,
только уже с приставкой services.
"""
