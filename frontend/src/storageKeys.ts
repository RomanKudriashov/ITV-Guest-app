/**
 * КЛЮЧИ БРАУЗЕРНОГО ХРАНИЛИЩА — ОДИН РАЗ, НА ВСЁ.
 *
 * Файл намеренно без единого импорта: его читают и приложение, и скрипты
 * съёмки, и E2E. Пока ключи жили по месту, они трижды расходились молча —
 * запись по чужому ключу законна, она просто ничего не значит:
 *
 *   `i18nextLng` вместо `itv.lang`      — скрипт снял четыре «языковых»
 *      комплекта, и все четыре оказались русскими.
 *   `itv.theme` вместо `itv.theme-mode` — кадры «в светлой теме» снимались в
 *      тёмной, и разницы никто не заметил, потому что её не было.
 *
 * Ни один случай не упал. Поэтому ключ теперь не пишут — его импортируют.
 */
export const STORAGE_KEYS = {
  /** Выбранная тема: `dark` | `light`. Читает ThemeProvider. */
  theme: 'itv.theme-mode',
  /** Выбранный язык. Читает детектор i18n. */
  language: 'itv.lang',
} as const;
