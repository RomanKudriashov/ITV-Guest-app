# Состояние работ

Документ для того, кто садится за проект с чистой головой. Отвечает на четыре
вопроса: что уже сделано, что лежит недоделанным и где, что ждёт решения
тек-лида, что ждёт ответа заказчика.

Чего здесь НЕТ: пересказа архитектуры (см. `overview.md`), принятых решений
(`decisions.md`) и дефектов с бэклогом (`ops/stand-defects.md`). Здесь только
состояние.

Обновлён: 18.09.2026 — по закрытию волны 9 (чат и ресепшен).

---

## Честная оговорка об источниках

Волны 1–3 изложены по репозиторию — истории коммитов, контрактам, бэклогу.
Волна 4 описана из первых рук. Границы волн — план тек-лида, в коде они не
размечены; если расхождение обнаружится, верить надо плану, а не этому
документу.

---

## Что сделано по волнам

### Волны 1–3 (до сентября 2026)

Собран продукт целиком в четырёх поверхностях: витрина гостя, трекер
персонала, панель отеля, консоль платформы. 168 путей API, четыре роли со
своей авторизацией, мультиарендность на RLS.

Крупные куски, по следам истории:

* **GRMS** — управление оборудованием номера: контракты, транспорт, коннектор
  как отдельная служба, диагностика, план номера с зонами, эмулятор iRidi для
  стенда. Ветки `grms/g0`…`grms/g13`.
* **Каталог и заказы** — витрина, корзина, оформление, трекер заказов с ручным
  порядком и откатом, типизированные доски по типам сервиса, аналитика,
  журнал уведомлений и правила эскалации.
* **Чат и поддержка** — переписка гостя с персоналом, сессии поддержки.
* **Платформа** — заведение отелей, тарифы, флот, белая метка, группы отелей,
  справочники, консоль с ролями.
* **Разбор на приложения** — `refactor/p0`…`p7`: страховочная сетка, схемы,
  каталог, отели, GRMS, тесты.
* **Эксплуатация** — прод-компоуз, выгрузка на стенд, офбординг с очисткой
  хранилища, i18n на четыре языка со сторожем паритета ключей.

### Волна 4 — оформление отеля и ускорение набора (партия 7)

Влито в `main` (партия 7, заходы 1–2):

* акцент отеля и стиль поверхности наконец доходят до витрины — до этого
  меняли только редактор;
* серверный показ витрины: те же сборщики, без гостевой сессии, настоящие
  экраны на настоящих ширинах;
* показ перестал зашивать «кухню» — заведение выбирает сервер, и если гостевых
  заведений нет, экрана у гостя нет тоже.

Остальное (заходы 3, г1, г2 + измерение и ускорение) доехало 15.09.2026 —
fast-forward из `partiya/7-preview`, 16 коммитов:

* **размер и цвет текста** — шкала кеглей через CSS-переменные, доходит до всех
  букв витрины, контраст текста проверяется числом (4.5:1);
* **паттерны фона** — выбирались, но гость не видел их ни разу; теперь фактура
  реально рисуется, TS-двойник питоновского справочника под сторожем реестров;
* **свой шрифт отеля** — загрузка, проверка подписи файла, `@font-face` с
  `font-display: swap`, и проверка спрашивает не «файл загрузился», а
  «буквы нарисованы им»;
* **черновики, версии, откат** (г1) — `BrandVersion` с номером, автором и
  базовой версией; устаревание черновика считается по данным, а не по флагу;
  при публикации устаревшего — явное подтверждение;
* **расписание публикаций** (г2) — таблица заданий `ScheduledJob`, служба
  `run_scheduler` отдельным процессом в compose, проверка раз в минуту,
  время в часовом поясе отеля, отметка опоздания, отмена, три события
  уведомлений; служба ВИДНА в консоли платформы по сердцебиению;
* **измерение и ускорение набора проверок** — итог ниже.

Числа ускорения (замер парой проходов подряд на одной машине):

    бэкенд   37 мин → 9 мин 37 с и 10 мин 19 с   1299 из 1299, дважды подряд
    e2e      41 мин 31 с → 33 мин 37 с           по времени проверок −22%

Ни одна проверка не удалена, ни одна параметризация по ширинам и языкам не
урезана: 1299 на бэкенде и 483 в e2e — ровно столько же, сколько до правок.

### Волна 5 — номерной фонд

Предмет: сам фонд, его свойства и то, как с ним работают пачкой. Занятость в
волну НЕ вошла и войти не могла — без PMS её неоткуда взять, а поле, которое
никто не обновляет, показывает неправду лучше, чем её отсутствие.

Сделано, по порядку от фундамента:

* **порядок и листание** — служебный ключ `Room.sort_key` (группы цифр
  дополняются нулями), `12` больше не встаёт после `101`; экран листает по 50 и
  говорит «показано 51–100 из 314». До этого он молча показывал первую сотню;
* **переименование с подтверждением** — наклейка QR кодирует НОМЕР, а имя
  устройства iRidi собирается из номера шаблоном. Оба последствия невидимы из
  интерфейса, поэтому смена номера требует `confirm_rename` и показывает
  разбор: какая ссылка умрёт, на какое имя уедет устройство. Отдельно —
  «оставить прежнее имя устройства», чтобы оборудование не тронулось;
* **имя удалённого номера освобождено** — ограничение в базе сужено до живых
  строк; повторное заведение ВОЗВРАЩАЕТ прежнюю комнату с её историей и гасит
  доступ прежнего проживания (сессии, PIN). На стенде таких удалённых номеров
  оказалось 35 038 при девяти живых;
* **поля фонда** — категория (свой справочник в `hotels`, к `grms.RoomType` не
  привязана: тот про оборудование и включается тарифом), состояние уборки,
  «вне продажи» ОТДЕЛЬНЫМ флагом (`is_active` — это вход гостя и печатный лист
  QR), ключ порядка этажей;
* **заведение пачкой с буквенными номерами** — «101-105, 3А, Люкс-1»;
  предпросмотр отдельной ручкой, которая физически не может создавать, предел
  проверяется до записи (опечатка «1-99999» не создаёт ничего);
* **массовая правка по ВЫБОРКЕ** — «выбрать все 314 в выборке» строит множество
  на сервере по тем же фильтрам, что и список; варианта «все на странице» нет
  намеренно. Номер пачкой не меняется;
* **сетка кубиков** — этажи один под другим, пропуски нумерации пустыми
  ячейками, фильтры ГАСЯТ, а не убирают. Цвет отдан занятости и потому не
  используется: кубики серые, легенда объясняет почему; уборка, «вне продажи»
  и связь с оборудованием — значками. Клик открывает панель справа, сетка
  остаётся на экране;
* **разрез аналитики по категории** — категория пишется СНИМКОМ в момент
  заказа; «без категории» показана и объяснена; сравнение категорий даётся на
  номер («люкс заказывает втрое чаще стандарта»), потому что абсолютные числа
  несравнимы.

Полные прогоны по закрытию волны (16.09.2026), оба — целиком:

    бэкенд   1340 passed, 2 failed, 7 skipped   14 мин 53 с
    e2e      493 из 493                         29 мин 12 с

Обе красных бэкенда — GRMS под нагрузкой (пункт 27): поодиночке зелёные за
46 с. Первый полный e2e дал 491 из 493: красная про цвет текста в показе бренда
поодиночке зелёная, вторая была настоящим дефектом сетки (рамка выделения
обрывалась у края) — починен, после чего второй полный проход зелёный целиком.

### Волна 6 — уведомления и контакты

Предмет: что система сообщает сотрудникам, кому, каким текстом и как это
доставляется; куда сотруднику можно написать. Сам бот, приём входящих и адаптер
Max в волну НЕ вошли — ждут заказчика.

Сделано, по порядку от фундамента:

* **справочник событий** — код, а не таблица: событие заводим мы, отель его
  настраивает. Восемь событий, только те, что система порождает: просрочка,
  отмена, сообщение гостя, низкая оценка, три события оформления,
  недоставленное уведомление. Тексты на четырёх языках, адресат по умолчанию;
* **журнал событий** — отдельные таблицы факта и доставок, факт пишется до
  отправки и остаётся при «некому отправить». Эскалация осталась на своём
  журнале: заказ там обязателен;
* **язык получателя** — личный канал говорит на языке владельца, общий — на
  языке отеля; текст отеля на другом языке язык получателя не перебивает;
* **настройки событий** — вкл/выкл, кому, какими видами каналов, свой текст;
  превью «вот так придёт» на ПОСЛЕДНЕМ НАСТОЯЩЕМ случае отеля, а без случая —
  прямое «примера нет», ничего не выдумывается;
* **по умолчанию включено только требующее действия** — просрочка, отмена,
  низкая оценка, недоставленное; «Кристалл» включает сообщения гостя своей
  настройкой;
* **отправка** — «когда» решает служба расписания (ступени эскалации),
  отправляет Celery после коммита, с нарастающей паузой повторов. Прежние
  флаги паузы не действовали (пауза была постоянной, 180 с), см. пункт 33;
* **служба расписания видна** — пульс с разбивкой по видам заданий и «сколько
  ждёт»;
* **контакты сотрудника** — телефон ожил (видят сам сотрудник и администратор
  отеля, управляющий — нет); Telegram и Max с отметкой подтверждения,
  заполняются только привязкой; профиль открыт и линейному персоналу;
  «подключить» без бота выключено и говорит «пока недоступно»;
* **одноразовые коды привязки** — своя таблица, только хэш, 10 минут,
  действует последний, обмен гасит атомарно платформенным подключением;
* **попутно** — низкая оценка уходит только руководителю отдела (пункт 33
  заказчика), девять кодов журнала переведены, домены `brand`, `room`,
  `notification`, `staff` под сторожем.

Полные прогоны по закрытию волны (16.09.2026), оба — целиком:

    бэкенд   1464 passed, 0 failed, 7 skipped   11 мин 12 с
    e2e      500 из 501                         34 мин 36 с

Красная e2e — свой шрифт оформления (`brand-custom-font:48`), поодиночке
3 из 3; записана к пункту 30. Первый полный бэкенд этого захода дал одну
красную — сторож контрактов не принял запись адреса `{telegram|max}` в
документе; документ поправлен, повторный прогон зелёный целиком.

### Волна 7 — локации и самовывоз (пункт 26 заказчика)

Предмет: где гость получает заказ и как — несут или забирает сам; чья это
стойка; что видит исполнитель.

Сделано, по порядку от фундамента:

* **матрица «категория × локация» стала правилом** — одно для списка мест и
  для заказа: у категории есть связки — только отмеченные места; связок нет —
  все места доставки, но не точки выдачи. Место должно подходить всей корзине
  (или своей части). Заказ мимо матрицы — `422 location_not_available`;
* **чья стойка — решает матрица**, а не привязка к отделу. Связка значит
  «доступно здесь»; поле способов связки (`delivery_modes`) больше не читается
  и помечено в коде мёртвым — удалить следующей волной;
* **вид локации «точка выдачи»** — плата и уточнение у неё запрещены формой и
  сервером; смена вида при открытых заказах разрешена (способ получения уже
  записан в заказе). В демо «Кристалла» — стойка лобби-бара, выдаёт коктейли
  и напитки бара;
* **способ получения следует из места** и фиксируется в заказе; зашитый
  список `["delivery","pickup"]` с индексом `[0]` убран совсем — сервер
  витрину не слушает;
* **место — по каждому заведению** в корзине из двух: общее место по
  умолчанию, спрашиваем один раз; по частям — только когда общего места нет
  или гость сам выбрал «в разные места». У каждой части свой номер (`parts`);
* **плата за доставку при выдаче не берётся** — даже если в строке локации
  она есть; у корзины из двух частей платит только часть, которую несут;
* **статусы по способу** — у статуса второе название: «Готово к выдаче»,
  «Выдано». Колонка доски — оба через « / »; карточка, следующие шаги, журнал
  и уведомления — по способу своего заказа;
* **признак «Выдача» в карточке трекера** стоит ПЕРЕД местом, номер гостя —
  только «кто придёт»: «Выдача: Стойка лобби-бара · заберёт гость из 305»;
* **четыре смысла «pickup» разведены** — в коде слово значит только
  самовывоз; медиана реакции — `median_accept_minutes` («Берут в работу
  за»); трансфер `airport-pickup` и «Подача» такси не тронуты;
* **попутно** — удаление локации убирает её связки (на стенде вычищено 129
  мёртвых); выключение и удаление сотрудника закрывают его сессии (и у
  команды платформы); прогон e2e выходит из всех своих входов в конце, уборка
  стенда гасит помеченные входы прогонов как страховку.

Полные прогоны по закрытию волны (17.09.2026), оба — целиком:

    бэкенд   1498 passed, 0 failed, 7 skipped   12 мин 56 с
    e2e      506 из 506, двумя заходами          ≈14,5 + 28,8 мин

Полный e2e одним заходом система сняла по памяти на 203-м тесте из 506 — до
него без красных. Остаток (305 тестов, 46 файлов с `guest-pickup-point`)
прогнан вторым заходом: 305 из 305. Сессии: второй заход вышел из 157 входов
и не оставил ни одного; 116 живых — от снятого первого, до выхода он не
дошёл; они помечены и закрываются уборкой по порогу 3 ч.

### Волна 8 — отзывы (пункты 29 и 33 заказчика)

Предмет: гость оценивает закрытый заказ, отель видит, разбирает и отвечает.

Сделано:

* **низкая — две звезды и ниже** (решение заказчика); отели на заводской
  тройке переведены миграцией. Сбор выключен — отзыва нет и мимо витрины;
* **у гостя одна карточка «оцените»** на главной — по последнему закрытому
  заказу, закрывается; живой заказ не предлагается никогда;
* **раздел «Отзывы»** (в «Оперативно»): фильтры по заведению, оценке, датам и
  статусу разбора, средняя по дням, очередь «ждут разбора». Администратор
  видит все, руководитель — своих заведений; отзыв о заказе из двух
  заведений принадлежит каждой части;
* **низкая оценка — событие сразу**, руководителю каждой части;
* **расследование на одном экране**: заказ, части, кто вёл, реакция и работа,
  была ли просрочка, сработавшие эскалации, история, чат гостя за время заказа;
* **разбор**: новый → разбирается → закрыт, закрыть — только со словами;
* **ответ гостю**: живая сессия — в его чат, мёртвая — только хранится, и
  экран говорит это до ответа;
* **снимки** — порог просрочки в заказе, порог «низкая» на отзыве (правило 4
  ниже);
* **утечка чата закрыта**: тред принадлежит сессии, а не номеру — следующий
  гость номера больше не читает переписку прежнего (было проверено фактом);
* **попутно** — вкладка «Отзывы» аналитики показывала «—» при живых данных и
  пустую таблицу; демо «Кристалла» получило две низкие оценки через сервис.

Полные прогоны (17.09.2026):

    бэкенд   1533 passed, 0 failed, 7 skipped   19 мин 33 с
    e2e      511 из 513, двумя заходами          14,7 + 34,3 мин

Две красные e2e:
`tracker-order-and-rollback:221` — отдельно 3 из 3; `new-service-to-guest:58`
(оба варианта, «Ресторан» и «Свой») — отдельно 10 из 14. Второй упирается в 60-секундный предел, когда
бэкенд отвечает 14–22 с: своп хоста занят на 18,1 из 19,4 ГБ, и страницы
виртуальной машины Docker подкачиваются. Вхолостую ответ ≤ 0,6 с, через API
всплеск не воспроизводится. Записано к пункту 35. Сессии: прогон вышел из всех
своих входов (новых живых — 0).

### Волна 9 — чат и ресепшен (пункты 10, 30, 31 заказчика)

Предмет: гость пишет одному адресату, ресепшен — единственная точка входа:
отвечает сам, оформляет заказ за гостя или передаёт задачу в отдел.

Сделано:

* **чат читают ресепшен и администратор** — раньше все треды отеля со всеми
  сообщениями открывались любому, кто вошёл в трекер (повар, горничная).
  Список листается курсором; пустые треды в него не попадают;
* **пустые треды не заводятся** — главная считает непрочитанные без треда
  (удалено 4064 пустых); каналы демо переименованы в «Уведомления: …»;
* **гость пишет ресепшену** — точка с кодом `reception`; прежние переписки
  перевешены (16); гостю отвечает ОТДЕЛ, имя сотрудника он не видит;
* **признак «есть каталог»** (пункт 10): у ресепшена нет меню и коммерции,
  разделы и своя коммерция ему не заводятся, на витрине и в поиске его нет;
* **рабочее место ресепшена** `/tracker/desk`: диалоги (непрочитанные сверху,
  дальше по последнему сообщению гостя, «ждёт N минут» красным), переписка,
  карточка гостя (номер, язык, заказы в работе, история за проживание, низкие
  оценки, переданные задачи);
* **взятие диалога** — «отвечает Дарья» у остальных, не блокировка;
  освобождается отпусканием, закрытым входом или 15 минутами тишины;
* **заказ от имени гостя** — каталог из диалога, матрица мест и самовывоз,
  пометка «оформил Ресепшен» у гостя и имя сотрудника в трекере, сообщение
  гостю в чат; оплата не трогается (её нет);
* **задача в отдел «Поручением»** — служебная позиция отдела: обычный заказ по
  его доске со статусами и эскалацией, гостю не видна нигде (каталог, поиск,
  витрина, аналитика как продажа). Переписка остаётся у ресепшена;
* **молчащий чат** — порог настройкой отеля (1–240 мин): сигнал смене, затем
  руководителю, через справочник событий волны 6; **часы ресепшена** из
  расписания сервиса (демо — 07:00–23:00), без расписания обещаний не даём;
* **попутно найдено и закрыто**: у сессии гостя заводилось два треда (гонка
  первого открытия и сокета) — ответ не доходил вживую; `create_order` едва не
  потерял транзакцию; служебная позиция поднимается, если её удалили уборкой.

### Волна 10. Погода, автоперевод, баннер (в работе)

* **Пункт 22 — город вместо координат.** Отель выбирал широту и долготу
  числами и не выбирал: координаты были у одного отеля из двадцати девяти.
  Теперь подсказка справочника Open-Meteo, координаты и переводы названия
  приезжают сами, рядом живое превью погоды, единицы (°C/°F) — настройка
  отеля. Попутно нашлись две вещи весомее самой правки: настройки главной не
  сохранялись вовсе (имя отеля лежало строкой вместо карты переводов — 422), и
  геокодер не ищет по-английски русское название, переводы берутся отдельным
  запросом по идентификатору города.
* **Пункт 20 — рекламный баннер.** Картинки каруселью, размер S/M/L, место
  вверху или внизу витрины, действие (ссылка, наше заведение, страница с
  описанием), правила показа (даты, время суток с окном через полночь, только
  первый вход, категории номеров, язык гостя), приоритет, один баннер на экран
  и всегда закрывается. Статистика — показы, клики, переходы, CTR — на
  карточке баннера в разделе «Маркетинг», НЕ в аналитике: реклама не продаёт, и
  рядом с выручкой её числа читались бы как канал продаж.

  Попутная находка: связь «номер → категория» жила с волны 5, но на стенде
  было 0 категорий и 0 размеченных комнат из 35 — правило показа по категориям
  работало бы вхолостую. Демо-сиды теперь заводят «Стандарт», «Делюкс», «Люкс»;
  миграцией чужие базы не трогаем — тарифные категории придумывает отель.

* **Пункт 32 — автоперевод без модели.** Построено всё, кроме вызова: очередь
  и прогон с отчётом, отметка «переведено машиной», правка человека сильнее
  машины, имена собственные по правилу сида, учёт расхода по месяцам отеля,
  честный счётчик охвата. Модель подключается заменой слоя —
  `TRANSLATION_PROVIDER`. Подробности: `docs/ops/translate.md`.

  Что мерили на стенде: 815 значений отеля «Кристалл», английский — 296,
  арабский и китайский — по 393. Русского под ключом `en` сейчас нет ни
  одного, но колонка «похоже на чужой язык» сторожит ручной ввод в CMS.

Полные прогоны по закрытию волны 10 (18.09.2026):

    бэкенд   1673 passed, 0 failed, 7 skipped        11 мин 57 с
    e2e      526 из 526, четырьмя заходами           9,6 + 10,2 + 6,7 + 7,2 мин

Единственная красная захода 1 — сторож контракта CMS: `/cms/banners` отвечал
403 «модуль выключен», и сторож считал это недоступной ручкой. Поправлен
сторож (гейт модуля — не расхождение контракта) и форма выдачи трёх новых
списков. Третий заход система сняла по памяти на старте — делили на четыре.

Полные прогоны по закрытию волны 9 (18.09.2026):

    бэкенд   1604 passed, 0 failed, 7 skipped   12 мин 09 с
    e2e      519 из 520, тремя заходами         15,8 + (снят по памяти) + 11,9 мин

Красная e2e — `sticky-stack:245` на странице заведения; поодиночке 3 из 3,
записана к пункту 35 (память машины). Второй заход система сняла по памяти на
160-м тесте из 309 — делить пришлось на три.

---

## Что в работе и на какой ветке

Партия 7 целиком в `main` (`251b3d9`). Волна 5 — ветка
`partiya/8-room-fund`, волна 6 — `partiya/9-notifications` поверх неё,
«Мои входы» — `partiya/10-profile-sessions`, волна 7 — `partiya/11-pickup`,
волна 8 — `partiya/12-reviews`, волна 9 — `partiya/13-chat`.
Все запушены и ждут слияния тек-лидом. После слияния — перезапустить воркер и
службу расписания руками (автоперезагрузка только у бэкенда).

---

## Что ждёт решения тек-лида

1. **Возвращать ли сон в 16 файлов e2e** (пункт 29 бэклога). Ожидание раскладки
   там стоит дороже сна на 147 с по всему набору; взамен файлы перестали
   зависеть от скорости машины. Список адресный, работа на полчаса.
2. **Ревизия 252 проверок с полной изоляцией** (пункт 26) — 19% набора, и это
   вся оставшаяся цена подготовки: 136 дорогих севов из 944.
3. **Ревизия настроек бренда замером на витрине** (пункт 24) — три настройки
   подряд оказались нерабочими при зелёных проверках; сколько ещё, неизвестно.
4. **Коннектор под нагрузкой** (пункт 27) — теперь и на бэкенде: полный прогон
   даёт две красных в GRMS (`assert 'offline' == 'confirmed'`), те же
   поодиночке — зелёные за 46 с. Воспроизводить дешевле на бэкенде, чем в e2e.

Приоритет 1 в бэклоге один: **токен обновления не ротируется** (пункт 25).
Украденное значение живёт вечно и переиспользуется, владелец не узнаёт.

---

## Что ждёт ответа заказчика

Работа по этим пунктам не начата и начата быть не может — нужен ответ:

| Вопрос | Что заблокировано |
|---|---|
| **Эмулятор PMS** | сверка номерного фонда и статусов заселения с гостиничной системой; без эмулятора проверять интеграцию не на чем |
| **Старший смены** | роль и её права: кто видит чужие смены, кто закрывает чужие заказы, кому уходит эскалация |
| **Лицензия на погоду** | сейчас Open-Meteo; коммерческое использование требует ответа о лицензии или замены провайдера |
| **Модель автоперевода** | какой провайдер и за чей счёт. Механизм построен целиком (очередь, отчёт, отметки, расход, охват); подключение — один класс и строка `TRANSLATION_PROVIDER`. Пока не подключён, кнопка честно молчит, а поля остаются пустыми |
| **Бот для Telegram и Max** | учётки ботов. Готово всё, что от них не зависит: поля контактов, одноразовые коды и их обмен (`contacts.redeem_code`), экран «подключить». Не сделаны сам бот, приём входящих (решено: долгий опрос), адаптер Max. Telegram-адаптер ни разу не выполнялся (пункт 31) |

---

## Следующий шаг: после волны 9

Волна 9 закрыта (см. выше). Что стоит знать, прежде чем брать следующую:

* **Пункт 10 закрыт** (заведение без каталога), **30 и 31 — по чату и
  ресепшену закрыты**: один чат, ресепшен как точка входа, задача в отдел,
  заказ за гостя. Осталось от них то, что ждёт заказчика: бот Telegram/Max
  (приём входящих) и роль старшего смены.
* **Заявки с формой ресепшен не оформляет** — такси и уборку гость заполняет
  сам; ресепшен передаёт их «Поручением».
* **Корзина заказа за гостя — одного заведения**, как у гостя.

* **Мёртвое поле `ServiceLocation.delivery_modes`** — не читается с волны 7,
  сиды его не пишут. Удалить миграцией следующей волной.
* **«Подача» такси** — ключ переименован в `guest.order.rideAt`, текст тот
  же; `pickup` в коде теперь значит только самовывоз.
* **Число «Берут в работу за» панель отеля получает, но не показывает** —
  решено не выводить: без порога и сравнения это цифра без смысла. Вернуться,
  когда будет с чем сравнивать.
* **Старые входы прогонов без пометки** — прогоны до волны 7 не ставили
  метку `ITV-E2E`; решено не гоняться — гаснут сами за 7 суток.
* **Полный e2e не проходит одним заходом** — порог ≈200 тестов, пункт 35.

По волне 6:

* **Бот ждёт учёток заказчика.** Когда они придут: служба долгого опроса в
  compose, обработчик `/start <код>` → `contacts.redeem_code`, переменные
  `TELEGRAM_CONTACT_BOT` / `MAX_CONTACT_BOT` — и кнопка «подключить» оживёт
  сама. Перед этим — пункт 31 (адаптер не выполнялся, Markdown).
* **Личные контакты ещё не адресат уведомлений.** Привязанный Telegram
  сотрудника пока не превращается в его личный канал — это шаг вместе с ботом.
* **Перед волной 7 сделано:** мёртвые настройки Celery убраны, сторож не
  даст им вернуться (пункт 33); «Мои входы» листаются страницами, а проверка
  токена больше не закрывает живую сессию при переходе (пункт 32 — ждёт
  подтверждения полным прогоном); `Retry-After` у Telegram записан к пункту 31.
* **Сессии на стенде копятся от прогонов e2e** — входы без выхода, по
  1–1,6 тысячи в день, гаснут через 7 суток. Уборка стенда их не трогает;
  гасить ли — решение тек-лида.
---

## Как здесь работать, чтобы не наступить дважды

Всё замеренное и выстраданное лежит в двух местах:

* `docs/ops/stand-defects.md` — дефекты и бэклог, 37 пунктов, каждый с числами;
* граблях сессии (память проекта) — эксплуатация стенда и методика замеров.

Четыре правила, которые стоили дороже всего:

1. **«Элемент есть» не отвечает на вопрос «элемент выглядит».** Вид проверять
   вычисленным стилем на витрине, а не наличием узла в редакторе.
2. **Проверка, читающая состояние стенда, сама им становится.** Условие
   задавать явно и возвращать исходное.
3. **Оценку строить по поведению во время прогона, а не по пометкам в коде.**
   Две неверные оценки подряд по бэкенду (6–10 и 12–15 против замеренных 9:37)
   родились из одной этой ошибки.
4. **Настройка, по которой считают ПРОШЛОЕ, фиксируется снимком в момент
   события.** Иначе смена настройки переписывает историю: заказ, который был
   вовремя, становится просроченным. Три случая подряд — категория номера,
   порог просрочки заказа, порог низкой оценки отзыва. Заводя новую настройку,
   спросить: считают ли по ней то, что уже случилось?
