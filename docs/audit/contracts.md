# Контракты против кода

18 документов-контрактов против 168 путей API. Аудит только читал: расхождения записаны, не
исправлены.

**Главная цифра:** на контракты ссылаются **5 файлов тестов из 74**. Это значит, что контракт
может разъехаться с кодом, и прогон этого не заметит — расхождение обнаружит тот, кто будет писать
клиента по документу.

---

## 1. Покрытие

| | Сколько |
|---|---|
| Контрактов | 18 |
| Путей в API | 168 |
| Путей, упомянутых хотя бы одним контрактом | 110 |
| **Путей, не описанных ни одним контрактом** | **58** |
| Путей, описанных в контракте, но отсутствующих в API | **2** |

Метод: из контрактов извлечены все строки вида `/api/...`, `/cms/...`, `/guest/...` и сопоставлены
с путями OpenAPI после нормализации параметров (`{id}` → `{}`). Метод грубый: он видит путь, а не
поля и коды ошибок. Полей и кодов не сверял никто — см. §4.

**Цифры исправлены после проверки.** Грубость метода стоила дорого в обе стороны:
он не увидел, что контракт может писать путь относительно объявленного выше
префикса (отсюда 71 вместо 58), и посчитал расхождениями фронтовые маршруты и
вебсокеты (отсюда 6 вместо 2). Обе поправки — в §2.

### Кто ссылается на контракты в прогоне

| Тест | Контракт |
|---|---|
| `hotels/api/test_module_registry.py` | `docs/module-registry-api-contract.md` |
| `hotels/api/test_commerce_cms.py` | `docs/commerce-api-contract.md` |
| `hotels/api/test_platform_api.py` | `docs/platform-api-contract.md` |
| `catalog/api/test_item_data.py` | `docs/guest-api-contract.md` |
| `catalog/api/test_showcase.py` | `docs/guest-surface-api-contract.md` |

Из 18 контрактов упомянуты в тестах **пять**. И даже эта связь — ссылка в комментарии, а не
проверка: тест не сверяет ответ со схемой из документа.

Отдельно: `module-registry-api-contract.md` упомянут в тесте, но **такого файла в `docs/` нет**.
Ссылка убрана (`tests/hotels/api/test_module_registry.py`); сам механизм описания заслуживает и
стоит вторым в очереди — см. `docs/api-contracts.md`.

---

## 2. Контракты описывают то, чего в API уже нет

> **ПОПРАВКА ПОСЛЕ ПРОВЕРКИ.** Каждое из шести утверждений сверено с кодом
> поимённо. **Настоящих расхождений оказалось два, ложных — четыре.** Метод
> аудита (жадный поиск строк, похожих на путь) не различал эндпоинт, фронтовый
> маршрут внутри JSON и адрес вебсокета. Таблица ниже исправлена; выводы,
> сделанные из шестёрки, надо читать с этой поправкой.

| Путь из контракта | Контракт | Проверка фактом |
|---|---|---|
| `/cms/departments`, `/cms/departments/{id}` | `hotel-admin-api-contract.md` | **устарело**: пути нет в схеме, понятие переименовано в заведения (`/cms/services`). Раздел сокращён до поведения |
| `/guest/menu` | `guest-api-contract.md` | **устарело**: псевдоним снят, `tests/catalog/api/test_service_requests.py` проверяет его 404. Строка заменена на «псевдонима больше нет» |
| `/cms/dashboard` | `cms-navigation-contract.md` | **ложное срабатывание**: это ФРОНТОВЫЙ маршрут внутри поля `"to"` ответа навигации (`router.tsx:70`), а сам `/api/v1/cms/navigation` жив |
| `/v1/staff/chat/{id}` | `guest-surface-api-contract.md` | **ложное срабатывание**: живой вебсокет `ws/staff/chat/<uuid:thread_id>/` (`realtime/routing.py:25`) |
| `/v1/onprem/connector` | `grms/contracts/backend-connector.md` | **ложное срабатывание**: живой вебсокет `ws/onprem/connector` (`routing.py:28`), там же комментарий, почему без слеша на конце |
| `/cms/analytics` (как единый путь) | `analytics-api-contract.md` | **ложное срабатывание**: контракт перечисляет все десять путей, просто относительно объявленного выше префикса |

Устаревших документов, по которым нельзя написать клиента, было **два**, и оба
исправлены. Правило, выведенное из этой проверки, записано в
`docs/api-contracts.md`; сторож — `test_contract_endpoints_exist_in_the_api`.

---

## 3. Эндпоинты, не описанные ни одним контрактом

71 путь. По разделам: **CMS — 42**, **платформа — 27**, гость — 1, служебный — 1.

Крупные необслуженные области:

| Область | Путей | Замечание |
|---|---|---|
| `/cms/grms/*` | 16 | управление номером в панели: импорт, типы, элементы, план, публикация, откат. Контракты `grms/contracts/*` описывают гостевую сторону и коннектор, но не CMS |
| `/cms/analytics/*` | 10 | отчёты, выгрузки, дриллдаун |
| `/platform/*` | 27 | флот, тарифы, команда, узлы, шаблоны — почти вся платформенная консоль |
| `/cms/badges/*` | 2 | маркетинговые бейджи |

Платформенная консоль — самая заметная дыра: `platform-api-contract.md` существует, но описывает
малую часть из 31 пути.

---

## 4. Чего этот аудит НЕ проверял

Сверялись **адреса**. Не сверялись:

- **поля запросов и ответов** — контракт может называть поле `title`, а код отдавать `name`;
- **коды ошибок** — 422 против 400, `confirm_mismatch` против `invalid`;
- **обязательность** — контракт говорит «обязательное», схема допускает `null`.

Это сверка на порядок дороже: нужен разбор таблиц полей в 18 документах против схем OpenAPI.
Автоматизируемо: у нас есть машинная схема (`api.get_openapi_schema()`), и сверка «поля из
контракта существуют в схеме» пишется сторожем. Пока такого сторожа нет.

---

## 5. Документы, помеченные как расходящиеся с реальностью

### `docs/grms/iridi-probe.md` — помечен, и правильно

Документ прозвона сам говорит: ТЗ §7 требует GET с телом, а сервер одинаково принимает POST, и все
инвентаризационные прогоны сделаны POST'ом. В нём же предложение зафиксировать POST в контракте.
**Предложение не выполнено:** `grms/contracts/backend-connector.md` по-прежнему описывает метод по
ТЗ, а код ходит POST.

### Проверка на документы того же класса

Искал пометки «устарело», «расходится», «не соответствует» по всему `docs/` — **других таких
пометок нет**. Это не значит, что расхождений нет: значит, что их никто не пометил. Найденные в
§2 шесть расхождений не помечены нигде.

### Ссылки между документами

Проверил все ссылки вида `docs/*.md` внутри документации: **битых ссылок нет**.

**Поправка.** Аудит смотрел ссылки из документов и из тестов, но не из боевого кода — и пропустил
вторую такую же: `apps/hotels/api/platform/hotels.py` ссылался на тот же несуществующий
`module-registry-api-contract.md`. Обе ссылки убраны. Проверка теперь простая и должна повторяться:
каждый путь `docs/*.md`, упомянутый в `apps/` и `tests/`, обязан существовать.

---

## 6. Что с этим делать — по убыванию отдачи

Аудит ничего не менял; список — предложения. Пункты 1 и 2 **сделаны**, см. пометки.

1. ~~**Сторож «путь есть в API — есть в контракте»**~~ — **сделано наоборот и это важно**:
   сторож проверяет, что каждый адрес ИЗ КОНТРАКТА существует в API
   (`test_contract_endpoints_exist_in_the_api`), а не что каждый путь API описан.
   Обратное направление краснело бы на 58 путях с первого дня, то есть было бы выключено;
   выбранное ловит ровно тот класс, из-за которого документы пережили свои эндпоинты.
2. ~~**Пометить шесть найденных расхождений**~~ — **сделано**: два настоящих исправлены в
   документах, четыре ложных разобраны в §2. Правило, по которому это решалось, записано в
   `docs/api-contracts.md`.
3. **Закрыть контрактом платформенную консоль и CMS GRMS** — 43 пути из 71.
4. **Сверка полей** — сторож «каждое поле из таблицы контракта есть в схеме». Дороже, но именно
   поля разъезжаются молча.
