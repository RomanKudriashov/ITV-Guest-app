{
  "name": "itv-guest-app-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E поверх поднятого стенда (docker compose up)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install chromium",
    "smoke": "playwright test --config stand-smoke/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
